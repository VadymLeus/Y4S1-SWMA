@startuml

|Користувач|
start
:Переходить на /support/new;
:Заповнює форму тікету;
:Натискає "Відправити";

|Система (Backend)|
:Створює тікет (status=new);

' Починаємо цикл обробки,
' який триває, поки тікет не буде закрито
while (Тікет не закрито?) is (Так)
  |Адміністратор|
  :Переглядає тікети;
  :Обирає тікет;
  :Відповідає на тікет;

  |Система (Backend)|
  :Оновлює статус на 'answered';

  |Користувач|
  :Переглядає відповідь;

  if (Проблема вирішена?) then (Так)
    :Закриває тікет;
    |Система (Backend)|
    :Оновлює статус на 'closed';
    ' Виходимо з циклу
    break
  else (Ні)
    :Додає коментар;
    |Система (Backend)|
    :Оновлює статус на 'in_progress';
    :Повідомляє адміністратора;
    ' Цикл продовжується (повертається на початок 'while')
  endif
endwhile

|Система (Backend)|
stop

@enduml